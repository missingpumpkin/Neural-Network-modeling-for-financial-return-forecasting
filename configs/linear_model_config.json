{
    "model_type": "linear",
    "input_dim": "auto",
    "output_dim": 1,
    "training": {
        "epochs": 100,
        "batch_size": 64,
        "learning_rate": 0.001,
        "weight_decay": 0.0001,
        "early_stopping_patience": 10
    },
    "data": {
        "window_size": 20,
        "prediction_horizon": 1,
        "train_ratio": 0.7,
        "val_ratio": 0.15,
        "normalize": true
    },
    "evaluation": {
        "metrics": ["mse", "rmse", "mae", "r2", "directional_accuracy", "information_coefficient", "sharpe_ratio"],
        "visualizations": ["predictions", "returns_over_time", "cumulative_returns"]
    }
}
